diff a/kernel/power/suspend.c b/kernel/power/suspend.c	(rejected hunks)
@@ -23,6 +23,7 @@
 #include <linux/slab.h>
 #include <linux/suspend.h>
 #include <linux/syscore_ops.h>
+#include <linux/ftrace.h>
 #include <trace/events/power.h>
 #ifdef CONFIG_LGE_LOG_SERVICE
 #include <linux/rtc.h>
@@ -240,6 +241,7 @@ int suspend_devices_and_enter(suspend_state_t state)
 			goto Close;
 	}
 	suspend_console();
+	ftrace_stop();
 	suspend_test_start();
 	error = dpm_suspend_start(PMSG_SUSPEND);
 	if (error) {
@@ -268,6 +270,7 @@ int suspend_devices_and_enter(suspend_state_t state)
 	}
 #endif
 	suspend_test_finish("resume devices");
+	ftrace_start();
 	resume_console();
  Close:
 	if (suspend_ops->end)
